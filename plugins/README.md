# Worldline Plugins

Plugins allow integrators to add bespoke proof generation logic while leveraging
the shared registry and compatibility façade. The sample plugin included here is
simple but demonstrates the expected interface.
