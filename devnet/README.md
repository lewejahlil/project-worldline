# Worldline Devnet

The devnet is a lightweight orchestration script that spins up an Anvil instance,
deploys the Worldline contracts, and seeds sample registry data. It is intended
for local experimentation and e2e testing.

Run with:

```bash
npm run devnet
```
